{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build AVR",
            "type": "shell",
            "command": "cmake --build build"
        },
        {
            "label": "Flash AVR",
            "type": "shell",
            "command": "cmake --build build --target flash"
        }
    ]
}